import React from 'react';

const ScanProgress = () => (
  <section className="scan-progress">
    <h2>Scanning in Progress</h2>
    <p>Hold still while we analyze your facial features and skin texture.</p>
    {/* Add animated loader or progress bar */}
  </section>
);

export default ScanProgress;